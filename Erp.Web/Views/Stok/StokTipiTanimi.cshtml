@model List<string>

@{
    ViewData["Title"] = "Stok Tipleri Tanımı";
}

<h1><i class="fas fa-tags"></i> Stok Tipleri Tanımı</h1>
<hr />

<div class="card">
    <div class="card-body">
        <div class="mb-3">
            <div class="input-group">
                <input type="text" id="newItem" class="form-control" placeholder="Yeni Stok Tipi Ekle" />
                <button class="btn btn-primary" onclick="addItem()"><i class="fas fa-plus"></i> Ekle</button>
            </div>
        </div>
        <table class="table table-bordered" id="tanimTable">
            <thead>
                <tr>
                    <th>Stok Tipi</th>
                    <th>İşlemler</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Any())
                {
                    foreach (var item in Model)
                    {
                        <tr>
                            <td>@item</td>
                            <td>
                                <button class="btn btn-sm btn-danger" onclick="deleteItem('@item')"><i class="fas fa-trash"></i></button>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="2" class="text-center text-muted">Henüz stok tipi tanımlanmamış.</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<div class="mt-3">
    <a asp-action="Index" class="btn btn-secondary">Stok Yönetimine Dön</a>
</div>

@section Scripts {
    <script>
        function addItem() {
            var val = $('#newItem').val().trim();
            if (!val) {
                alert('Lütfen bir değer giriniz.');
                return;
            }

            $.ajax({
                url: '/Stok/StokTipiTanimiEkle',
                type: 'POST',
                data: { item: val },
                success: function () {
                    location.reload();
                },
                error: function () {
                    alert('Ekleme sırasında bir hata oluştu.');
                }
            });
        }

        function deleteItem(item) {
            if (!confirm('"' + item + '" silinsin mi?')) return;

            $.ajax({
                url: '/Stok/StokTipiTanimiSil',
                type: 'POST',
                data: { item: item },
                success: function () {
                    location.reload();
                },
                error: function () {
                    alert('Silme sırasında bir hata oluştu.');
                }
            });
        }
    </script>
}